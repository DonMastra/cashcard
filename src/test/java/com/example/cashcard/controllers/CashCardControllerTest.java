package com.example.cashcard.controllers;

public class CashCardControllerTest {
    /*@Test
    void shouldReturnCashCardForExistingId() {
        CashCardController controller = new CashCardController();
        ResponseEntity<CashCard> response = controller.findById(99L);
        assertEquals(org.springframework.http.HttpStatus.OK, response.getStatusCode());
        CashCard body = response.getBody();
        assertNotNull(body);
        assertEquals(99L, body.id());
        assertEquals(123.45, body.amount());
    }

    @Test
    void shouldReturnNotFoundForNonExistingId() {
        CashCardController controller = new CashCardController();
        ResponseEntity<CashCard> response = controller.findById(1L);
        assertEquals(org.springframework.http.HttpStatus.NOT_FOUND, response.getStatusCode());
        assertNull(response.getBody());
    }*/
}
